{
  "name": "hirari",
  "version": "0.1.0",
  "description": "ひらり — スマホ向けドッジゲーム",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "bump": "npm version patch --no-git-tag-version"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vitest": "^2.1.8"
  }
}
